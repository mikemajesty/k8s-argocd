apiVersion: v1
kind: Service
metadata:
  name: {{ include "cat-app.fullname" . }}
  labels:
    {{- include "cat-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "cat-app.selectorLabels" . | nindent 4 }}
